import React, { FormEvent, ChangeEvent } from 'react';
import { Header } from '../../components/StartedPageComponents/Header';
import { Main } from '../../components/StartedPageComponents/Main';

export const StartedPage = () => {
    return (
        <div>
            <Header />
            <Main />
        </div>
    );
}